# nata 実装TODO

## 方針

- まずは個別コマンドより先に、CLIの共通基盤を実装する
- PDF処理は`qpdf`を外部コマンドとして利用する
- ユーザー向け仕様は`docs/command-spec.md`を基準とする
- 最初の縦切り実装対象は`extract`とする

## TODO

- [x] `Cargo.toml`に初期依存を追加する
  - 追加済み: `clap`, `thiserror`
  - `tempfile`, `serde`, `toml`は必要になった段階で追加する
- [x] CLI骨組みを作成する
  - `nata <command> [options]`の形を受けられるようにする
  - サブコマンド定義だけ先に揃え、処理本体は後回しにする
  - `--help`の表示確認済み
- [x] 終了コード定義を実装する
  - `0`: 成功
  - `1`: 一般エラー
  - `2`: CLI引数エラー
  - `3`: 入力PDFエラー
  - `4`: 出力ファイルエラー
  - `5`: 外部バックエンドエラー
  - `6`: strictモード違反
  - `Cli::try_parse()`でCLI引数エラー時に終了コード`2`を返すように調整済み
- [x] `qpdf`検出を実装する
  - 優先順は`--qpdf`→`NATA_QPDF`→`PATH`
  - 未検出時は適切なエラーと終了コード`5`を返す
- [x] ページ指定パーサを実装する
  - 対応: `1`, `1-5`, `1,3,5-8`, `all`, `last`, `odd`, `even`, `4-last`
  - 順序維持、重複許可、1始まりを前提にする
- [x] 入出力の共通検証を実装する
  - 入力PDFの存在確認
  - 出力先の衝突確認
  - 一時ファイル経由の出力
- [x] `extract`コマンドを最初に実装する
  - `--pages`必須
  - `-o, --output`必須
  - 指定ページだけを新しいPDFとして出力する
- [x] 実装前提メモを整理する
  - `docs/plan/implementation-assumptions.md` に実装判断を記録
- [ ] リポジトリ公開に伴うライセンスを決定する
  - [ ] 依存関係のライセンスを再度洗い出す
  - [ ] ライセンス決定

## 手動チェックポイント

- [x] 依存追加後に`cargo check`を実行する
- [ ] `qpdf`あり/なしの両方で検出挙動を確認する
  - `qpdf`なしの失敗ケースは確認済み
  - `qpdf`ありの成功ケースは未確認
- [x] `--help`とCLI引数エラーの終了コードを確認する
  - `--help`は表示確認済み
  - 不正オプション時の終了コード`2`を確認済み
- [ ] ページ指定パーサで代表入力を確認する
  - 単体テストは追加済み
- [ ] `extract`で最小ケースを手動確認する
  - 単体テストと`cargo check`は通過済み

## 後続候補

- [ ] `merge`
- [ ] `remove`
- [ ] `reorder`
- [ ] `insert`
- [ ] `replace`
- [ ] `split`
- [ ] `rotate`
